package Entidades;

import java.math.BigDecimal;

public class Apartamento extends Propriedade{

    public Apartamento(String endereco, BigDecimal preco) {
        super(endereco, preco);
    }
}
